{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

---

## 🎯 Resumo Visual:
```
seu-projeto/
├── package.json          ← SUBSTITUA por código acima
├── index.html            ← SUBSTITUA por código acima
├── vercel.json           ← CRIE NOVO com código acima
├── services/
│   └── geminiService.ts  ← SUBSTITUA por código acima
└── ... (resto dos arquivos)
